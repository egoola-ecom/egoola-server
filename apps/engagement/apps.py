from django.apps import AppConfig


class EngagementConfig(AppConfig):
    """Reviews and favourites (Section 3.10)."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.engagement"
    label = "engagement"
