from django.db import models  # noqa: F401
from apps.core.models import AuditedModel  # noqa: F401

# Models for this app are built out phase by phase against the
# Database Redesign document — Identity and login, plus the seller's business profile (Section 3.2, 3.3 of the DB Redesign document).
# Not yet built: see the Phase Tracker for which phase covers this app.
